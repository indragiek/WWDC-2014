#include "Pods-Mensa.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Mensa" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Mantle" "${PODS_ROOT}/Headers/Mensa"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}